import "mocha";
import { expect } from "chai";
var deploymentTests = require("../deployment/tests.json");
const lambda = require("../lambda.js");

describe("Requests", function() {
	it("TestEvent", async done => {
		done();
	});
});
